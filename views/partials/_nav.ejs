<nav class="site-nav">
  <div class="site-nav__desktop">
    <a href="/" class="site-nav__brand" aria-label="Go to homepage">
      <span class="site-nav__brand-light">Commencement</span>
      <span class="site-nav__brand-bold">2026</span>
    </a>

    <!-- Desktop nav links -->
    <div class="site-nav__links" aria-label="Primary navigation">
      <a href="#upcoming-commencement">Livestreams</a>
      <a href="#featured-awardees">Highlights</a>
      <a href="#featured-awardees">Speakers</a>
      <a href="#featured-awardees">Video</a>
      <a href="#featured-awardees">Social</a>
      <a href="#featured-awardees">Photos</a>
      <a href="#featured-awardees">Honors</a>
    </div>

    <!-- Desktop auth area -->
    <div class="site-nav__auth">
      <% if (user) { %>
        <span class="site-nav__avatar"><%= user.username.charAt(0).toUpperCase() %></span>
        <span class="site-nav__username"><%= user.username %></span>
        <a href="/awardees">Manage Awardees</a>
        <a href="/auth/sign-out">Sign Out</a>
      <% } else { %>
        <a href="/auth/sign-up">Sign Up</a>
        <a href="/auth/sign-in">Sign In</a>
      <% } %>
    </div>
  </div>

  <!-- Mobile header row -->
  <div class="site-nav__mobile-top">
    <a href="/" class="site-nav__brand site-nav__brand--mobile" aria-label="Go to homepage">
      <span class="site-nav__brand-light">Commencement</span>
      <span class="site-nav__brand-bold">2026</span>
    </a>

    <div class="site-nav__mobile-actions">
      <% if (user) { %>
        <span class="site-nav__avatar"><%= user.username.charAt(0).toUpperCase() %></span>
      <% } else { %>
        <span class="site-nav__avatar">T</span>
      <% } %>

      <!-- Visual menu button for mobile layout -->
      <button class="site-nav__menu-button" type="button" aria-label="Open menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </div>

  <!-- Mobile section label row -->
  <div class="site-nav__mobile-subnav">
    <span>Livestreams</span>
    <span class="site-nav__chevron">⌄</span>
  </div>
</nav>